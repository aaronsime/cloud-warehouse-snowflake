version: 2

models:
  - name: int__dim_order_status
    description: 'Contains location information'
    columns:
      - name: order_status_id
        description: 'Unique identifier for each order status'
        tests:
          - unique
          - not_null
      - name: order_status
        description: 'description of order status'
        tests:
            - not_null
